# Private repo. The whole brain lives here: full book texts, syntheses, the map,
# and the build helpers. The only boundary is "keep this repo private" (the book
# texts are copyrighted O'Reilly content). Nothing brain-related is excluded.

# OS and editor junk
.DS_Store
*.swp
*~

# Raw source PDFs (text already extracted into books/). Local only, keep out of git.
_incoming/
